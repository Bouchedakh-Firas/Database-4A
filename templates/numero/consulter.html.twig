{% extends 'base.html.twig' %}

{% block title %} Modifier Numéro
{% endblock %}
{% block stylesheets %}
    <link href="{{ asset('css/IdClient.css') }}" rel="stylesheet" />
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('css/IdClient.js') }}"></script>
{% endblock %}

{% block body %}

    <div class="form-body" >
        <div class=""  style="margin-top: 0px">
            {#            <div class="row"  style="margin-top: 0px">#}
            <div class="form-holder " style="min-height: 5vh;">
                <div class="form-content">
                    <div class="form-items">
                        <h3>Modifier une Numéro</h3>
                        <br>
                        {{ form_start(formn) }}
                        <div class="row g-3">
                            <div class="col-sm-7">
                                <label>Libelle</label>
                                {{ form_errors(formn.libelle) }}
                                {{ form_widget(formn.libelle) }}
                            </div>
                            <div class="col-sm">
                                <label>Code Pays</label>
                                {{ form_errors(formn.codepays) }}
                                {{ form_widget(formn.codepays) }}
                            </div>
                            <div class="col-sm">
                                <label>Numéro</label>
                                {{ form_errors(formn.numero) }}
                                {{ form_widget(formn.numero) }}                            </div>
                        </div>
                        <br>

                        <br>

                        {{ form_widget(formn.client) }}
                        <div class="form-button mt-3">
                            <button  type="submit" class="btn btn-primary">Modifier</button>

                        </div>
                        {{ form_end(formn) }}

                    </div>
                </div>
            </div>
        </div>
    </div>



{% endblock %}
